import { BoardConfig } from "../../src/GeometryBoard";

export const BOARD_SIZE = 50;

export const BASE_BOARD_CONFIG: BoardConfig = {
  xMin: -10,
  xMax: 10,
  yMin: -10,
  yMax: 10,
};

export const FIRST_QUAD_BOARD_CONFIG: BoardConfig = {
  xMin: 0,
  xMax: 10,
  yMin: 0,
  yMax: 10,
};
